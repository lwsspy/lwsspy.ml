Installation
------------


From Pypi

.. code-block:: bash

    pip install lwsspy.ml

From source

.. code-block:: bash

    cd path/to/repo
    pip install -e .

Using ``environment.yml``

.. code-block:: bash

    cd path/to/repo
    conda env create -f environment.yml
    conda activate ml
    pip install -e .


